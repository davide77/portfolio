/**
 * Three.js layer the lens cube-camera renders. Anything assigned to this
 * layer is invisible to the main camera but visible to the refraction lens,
 * which makes the lens act as a reveal mask over hidden geometry.
 */
export const LENS_ONLY_LAYER = 1;
